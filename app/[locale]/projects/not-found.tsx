// Nested boundary: notFound() thrown below this segment is rendered on the
// server, inside the locale layout, with a real 404 status.
export { default, generateMetadata } from "@/components/errors/LocalizedNotFound";
